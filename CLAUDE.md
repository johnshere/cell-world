# CLAUDE.md

Cell-World v1.0 神经网络涌现生态模拟器 - Claude Code 开发指南

> **完整设计文档**: [docs/DESIGN.md](docs/DESIGN.md)

## 常用命令

```bash
cargo build           # 编译项目
cargo run             # 运行项目
cargo build --release # 发布构建（启用 LTO 优化）
cargo fmt             # 格式化代码
cargo clippy          # 代码检查
```

## 核心模块

| 模块     | 文件                    | 职责                           |
| -------- | ----------------------- | ------------------------------ |
| 神经网络 | `src/neural/genome.rs`  | NEAT 基因组，结构变异，器官基因 |
|          | `src/neural/network.rs` | 神经网络前向传播               |
| 世界系统 | `src/world/world.rs`    | 主循环、感知、动作执行         |
|          | `src/world/creature.rs` | 生物结构                       |
|          | `src/world/energy.rs`   | 能量粒子                       |
|          | `src/world/trail.rs`    | 痕迹点系统                     |
|          | `src/world/spatial.rs`  | 空间索引 O(1) 查询             |
| 渲染     | `src/render/canvas.rs`  | 画布渲染、拖拽缩放、器官绘制   |
|          | `src/render/panel.rs`   | 侧边栏统计面板                 |
| 应用     | `src/app.rs`            | egui 应用主循环、日志、选中     |
| 配置     | `src/config.rs`         | 参数配置、环境温度             |
| 存储     | `src/store.rs`          | 生物模板存档（JSON）            |

## 关键设计

- **无限世界**: 无边界，视窗可自由拖拽缩放
- **火山 + 陨石能量**: 火山定期喷发（80秒/次，35粒子），陨石随机降落（40秒/次，16粒子）
- **器官基因系统**: 鼻子/双眼/嘴巴，各自可独立变异开关+功率基因(0~1)，影响感知和行为
- **器官冷却+按需计费**: 器官不再固定消耗，扫描/咬合时按 power² × cost 扣费，冷却期间保持缓存
- **感知系统（鼻子+双眼，带冷却）**:
  - 鼻子（正前方20°半角，探测距离=nose_power×视觉×1.5，冷却0.4s）: 强度叠加模型，5通道；痕迹仅鼻子可探测
  - 双眼（±50°方向，50°半角，探测距离=eye_power×视觉，冷却0.15s）: 距离模型，3通道×2
  - 自身状态: 3通道（能量/体温/环境温度），始终更新
- **14维输入**: 鼻子5 + 左眼3 + 右眼3 + 自身3
- **4维输出**: 转向角(0)、速度(1)、嘴(2，负=咬/正=喂/接触食物自动吸收)、繁殖(3)
- **痕迹点系统**: 移动消耗转化为痕迹点（能量守恒），可被吸收，衰减快于粒子
- **环境温度**: 距火山越近越热（三次方衰减 t³），温度流失+100%/自动回暖（无热代谢加成）
- **落地杀伤**: 火山/陨石粒子分批下落（火山6s/陨石3s），落地时砸死半径12内生物；下落中粒子不可食用、不衰减
- **火山中心富集**: u²分布，50%粒子在25%半径内
- **咬合系统**: 咬合力=|mouth|×mouth_power，伤害=战力比×bite_transfer_rate，咬有能量成本，冷却1s
- **战力系统**: 战力 = f(能量, 体温, 速度, 同族援助)，咬时攻方乘咬合力
- **物理约束**: 存在消耗（基础代谢+年龄倍率+体温逸散+环境温度+器官按需扣费）、繁殖成本（30%）、死亡条件（能量≤0）
- **统一变异率**: 所有变异逻辑共用同一个 `mutation_rate`（15%），器官变异率(含功率)为其1/10
- **祖先追溯聚类**: 沿 parent_id 追溯最老活祖先，相似度≥0.9 归入同族

## 开发注意

1. NEAT 变异: `genome.rs` → `mutate(rate)` 单一变异率控制所有变异（含器官布尔+功率变异）
2. 感知系统: `world.rs` → `compute_perception_with_cooldown()` 冷却触发+`compute_perception_inner()` 选择性扫描
3. 动作执行: `world.rs` → `execute_actions()` 4输出映射，嘴巴食物吸收不受冷却限制，咬/喂受冷却限制
4. 战力+咬合: `config.rs` → `combat_power()` 公式，`world.rs` → 咬时攻方战力×咬合力 vs 守方战力
5. 环境温度: `config.rs` → `ambient_temperature()` 三次方衰减(t³)，影响体温逸散/回暖（无热代谢加成）
5b. 落地杀伤: `world.rs` → `process_landings()` 下落粒子倒计时+落地砸死附近生物
6. 空间索引使用 FxHashMap，查询复用缓冲区避免分配（creature/energy/trail三套）
7. 种族聚类: `calculate_clan_cache()` 每秒更新一次
8. 器官差异影响相似度：每个不同器官扣0.05，功率差异额外扣0.02/器官
9. 所有复杂行为应是进化结果，避免硬编码

## 目标

现在，希望你启动程序。每隔段时间观察日志，必要情况下增加日志，用于调试。期望不通过硬编码，通过配置调试、神经学习
，让这个世界演化出采集、族群、哺育、捕猎等等，群体性的行为。
