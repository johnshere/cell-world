1. 可选，低于最小生命时停止演化
2. 粒子最小能量，小于后消失
3. 地形方案
4. 选中生命时显示神经网络相关信息和评分
5. 能量趋势图中没有显示生命个数的曲线，但是得注意显示时考虑数量级
6. 连接只增加不减少，算力消耗只会增加不减少。需要赫布法则控制削减甚至删除连接。现在的权重变化是随机的，权重就是突触（连接）的核心。是否可以认为要对其进行用进废退的处理
7. 关联上面6的问题，现在神经网络是否进化有效的判定在外界。即，生命是否能种群扩大，没有内在的神经网络奖励机制，类似多巴胺的。
8. 增加一个配置、功能，每个多少秒自动投放一个随机生命，不是基因库选取的

- 优势种与当前基因库中存在的进行比对，已经有相似度达到阈值时，不添加；现在似乎是更新，这是不对的
