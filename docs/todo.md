1、基因库的列表checkbox选中后，会定时投放，投放逻辑删除；选中checkbox在自动记录的模板不显示，改为显示；低于最低生命数量会自动投放，不再从自动记录中投放，改为从选中的投放。即两者结合取消定时投放，改为最低数量时投放选中模板
2、鼠标滑倒力导图连线时，应该显示线的信息
3、b25为什么会连接光嘴27output上，还连接到繁殖23上了，只回答
4、现在初始生命的节点数是36，拔高到初始48。每个input、output都至少连接一个节点（目标区域的），并且不允许input直连output。修改配置项里关于初始神经网络的配置，改为初始节点数，初始连接数
