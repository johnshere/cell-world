## 待办

节点力导图要把每个节点的id标注出来（如：12）
力导图滚轮缩放值减半
block内和外的节点最小距离要不一样，外是内的一倍，即外是现在的限制的2倍

发现output被联合区直连了，这不对，我设定是output只能被指定block连接；为了兼容，应调整不允许非指定block创建新得连接到output

育均:4581s│均:11094.3│中:10722.5│最长:33242.6│最短:0.7；这些显示数据只要获取现在存在的生命的数值就可以了

力导图弹框打开时计算缩放适应弹框尺寸是不合适的，因为力还没结束，需要慢慢几秒内缓慢缩小缩放
